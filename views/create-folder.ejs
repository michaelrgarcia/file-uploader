<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create folder</title>
  </head>
  <body>
    <h1>Create folder</h1>
    <% if (locals.currentUser) { %>
    <form action="/my-folders/create" method="post">
      <div class="form-row">
        <label for="folderName">Name: </label>
        <input type="text" name="folderName" id="folderName" />
      </div>
      <button type="submit" style="margin-top: 30px">Create</button>
    </form>
    <% } else { %>
    <p>
      You must <a href="/register">register</a> and/or
      <a href="/login">log in</a> to create a folder.
    </p>
    <% } %>
  </body>
</html>
